import { EnumDisplayPipe } from './enum-situacaoEquipamento-display.pipe';

describe('EnumDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new EnumDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
